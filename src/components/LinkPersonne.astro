---
import type { CollectionEntry } from "astro:content";

interface Props {
  personne: CollectionEntry<"personnes">;
}

const { personne } = Astro.props;
---

<a class="hover:underline" href={`/personnes/${personne.id}`}>{personne.data.nom}</a>
