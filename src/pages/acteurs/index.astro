---
import { getCollection } from "astro:content";
import Layout from "../../layouts/Layout.astro";

const acteurs = await getCollection("acteurs");
---

<Layout title="Acteurs">
  {acteurs.map((acteur) => acteur.data.nom)}
</Layout>
